# Studybot Antigravity reverse Gemini
# Use a hostname (e.g. *.sslip.io), not a raw IP — Cloudflare Workers reject direct IP fetch (error 1003).
REVERSE_GEMINI_BASE=http://152.42.220.127.sslip.io:8085/antigravity
REVERSE_API_TOKEN=your_reverse_api_token_here
REVERSE_GEMINI_MODEL=gemini-3.6-flash-medium

PORT=8787
ALLOW_DEMO_FALLBACK=true
DEMO_MODE=false
